import React from 'react';
import { shallow } from 'enzyme';



import Pages from '../pagination';

describe('List tests', () => {
    it('renders',()=>{
        const filter = swallow(<Pages />)
        
    })
})